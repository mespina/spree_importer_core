Description:
    Permite generar un nuevo mecanismo para importar datos, de modo que solo te preocupes de su implementación

Example:
    rails generate importer Thing

    This will create:
        app/models/spree/importer_core/thig_importer.rb

    Insert importer locales into:
        config/spree_importer_core.en.yml

    And update importers list at:
        config/initializers/spree.rb
