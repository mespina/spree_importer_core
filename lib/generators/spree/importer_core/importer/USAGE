Description:
    Will generate a new mechanism for importing data, so only worry for implementation

Example:
    rails generate importer Thing

    This will create:
        lib/spree_importer_core/thig_importer.rb

    Insert importer locales into:
        config/spree_importer_core.en.yml

    Add sample importer template
        lib/spree_importer_core/templates/thig.xlsx

    And update importers list at:
        config/initializers/spree.rb
